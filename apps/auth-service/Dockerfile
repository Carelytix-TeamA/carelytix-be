# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @carelytix-be/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t carelytix-be-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system carelytix-be-auth-service && \
          adduser --system -G carelytix-be-auth-service carelytix-be-auth-service

COPY apps/auth-service/dist carelytix-be-auth-service/
COPY apps/auth-service/package.json carelytix-be-auth-service/
RUN chown -R carelytix-be-auth-service:carelytix-be-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix carelytix-be-auth-service --omit=dev -f install

CMD [ "node", "carelytix-be-auth-service" ]
